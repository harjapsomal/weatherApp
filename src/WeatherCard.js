import React, { useState, useEffect } from "react";

function WeatherCard(props) {
  // date, picture of weather,
  class weathercard extends React.Component {
    render() {
      return;
    }
  }

  return <div>Hello weathercard {props.id}</div>;
}

export default WeatherCard;
